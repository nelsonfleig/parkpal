import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

export const ComplainBox = styled(Box)(() => ({
  display: 'flex',
  gap: '1rem',
  flexWrap: 'wrap',
}));
