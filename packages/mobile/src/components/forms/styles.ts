import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 5,
    paddingTop: 10,
    paddingBottom: 10,
    paddingLeft: 20,
    paddingRight: 20,
    fontWeight: 'normal',
  },
});

export default styles;
