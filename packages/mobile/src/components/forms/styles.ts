import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 5,
    paddingTop: 10,
    paddingBottom: 10,
    paddingLeft: 20,
    paddingRight: 20,
    fontWeight: 'normal',
  },

  input: {
    width: 200,
    backgroundColor: '#fff',
    marginBottom: 10,
  },
});

export default styles;
