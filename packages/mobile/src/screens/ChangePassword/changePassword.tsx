import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export const ChangePassword = () => (
  <SafeAreaView>
    <Text>Hello ChangePassword</Text>)
  </SafeAreaView>
);
