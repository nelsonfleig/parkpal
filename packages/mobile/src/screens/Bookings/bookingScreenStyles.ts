import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    textAlign: 'center',
    marginBottom: 30,
    marginTop: 20,
    color: 'black',
  },
});

export default styles;
