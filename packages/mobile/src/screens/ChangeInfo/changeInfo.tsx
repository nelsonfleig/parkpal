import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export const ChangeInfo = () => (
  <SafeAreaView>
    <Text>Hello changeinfo</Text>)
  </SafeAreaView>
);
