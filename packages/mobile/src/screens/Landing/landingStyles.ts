import { StyleSheet } from 'react-native';

export const landingStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  findHere: {
    position: 'absolute',
    top: 90,
    right: 20,
    left: 20,
    zIndex: 3,
  },
});
